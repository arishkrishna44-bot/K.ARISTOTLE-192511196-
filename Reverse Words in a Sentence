#include<stdio.h>
#include<string.h>

int main()
{
    char str[100];
    int i;

    fgets(str,100,stdin);

    for(i=strlen(str)-1;i>=0;i--)
    {
        if(str[i]==' ')
        {
            printf("%s",&str[i]+1);
            str[i]='\0';
        }
    }

    printf("%s",str);
}
