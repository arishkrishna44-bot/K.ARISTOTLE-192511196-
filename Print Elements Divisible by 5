#include <stdio.h>
int main() {
    int a[5], i;

    for(i=0;i<5;i++)
        scanf("%d",&a[i]);

    printf("Numbers divisible by 5:\n");

    for(i=0;i<5;i++){
        if(a[i] % 5 == 0)
            printf("%d ",a[i]);
    }

    return 0;
}
