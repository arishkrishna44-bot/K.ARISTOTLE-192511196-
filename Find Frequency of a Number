#include <stdio.h>
int main() {
    int a[5], i, num, count=0;

    for(i=0;i<5;i++)
        scanf("%d",&a[i]);

    printf("Enter number: ");
    scanf("%d",&num);

    for(i=0;i<5;i++){
        if(a[i]==num)
            count++;
    }

    printf("Frequency = %d",count);
    return 0;
}
