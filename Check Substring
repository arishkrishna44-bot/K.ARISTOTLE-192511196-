#include<stdio.h>
#include<string.h>

int main()
{
    char str[100],sub[50];

    scanf("%s%s",str,sub);

    if(strstr(str,sub)!=NULL)
        printf("Substring found");
    else
        printf("Substring not found");
}
