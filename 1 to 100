#include <stdio.h>

int main() {
    int num;

    printf("Enter a number: ");
    scanf("%d", &num);

    if(num >= 1 && num <= 100) {
        printf(" within range");
    }
    else {
        printf(" out of range");
    }

    return 0;
}
