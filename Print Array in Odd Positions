#include <stdio.h>
int main() {
    int a[5], i;

    for(i=0;i<5;i++)
        scanf("%d",&a[i]);

    printf("Odd position elements:\n");
    for(i=0;i<5;i+=2)
        printf("%d ",a[i]);

    return 0;
}
